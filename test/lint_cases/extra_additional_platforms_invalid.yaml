expected_failures:
  - extra_additional_platforms_invalid
recipes:
  one:
    package:
      name: one
      version: 0.1
    source:
      url: https://somewhere
      sha256: 123
    build:
      number: 0
      run_exports:
        - "{{ pin_subpackage('one') }}"
    test:
      commands:
        - do nothing
    about:
      license: BSD
      home: https://elsewhere
      summary: the_summary
    extra:
      skip-lints:
        - should_be_noarch_generic
      additional-platforms:
        - linux-aarch64
        - osx-x86_64
