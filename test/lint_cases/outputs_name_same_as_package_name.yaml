expected_failures:
  - outputs_name_same_as_package_name
recipes:
  meta_package:
    package:
      name: meta_package
      version: 0.1
    source:
      - url: https://somewhere
        sha256: 123
      - url: https://elsewhere
        sha256: abc
    build:
      noarch: True
      number: 0
      run_exports:
        # add a run export
        # (note that jinja templating as in reality is not
        # supported by our test system to the same
        # extend as in conda build)
        - "{{ pin_subpackage('meta_package') }}"
    outputs:
      - name: meta_package # offending entry
        build:
          noarch: True
          run_exports:
            # add a run export
            # (note that jinja templating as in reality is not
            # supported by our test system to the same
            # extend as in conda build)
            - "{{ pin_subpackage('meta_one') }}"
        test:
          commands:
            - do nothing
      - name: meta_two
        build:
          noarch: True
          run_exports:
            # add a run export
            # (note that jinja templating as in reality is not
            # supported by our test system to the same
            # extend as in conda build)
            - "{{ pin_subpackage('meta_two') }}"
        test:
          imports:
            - no imports to see here
    about:
      license: MIT
      home: https://elsewhere
      summary: another_summary
