expected_failures:
  - gpl_requires_license_distributed
recipes:
  one:
    package:
      name: one
      version: 0.1
    source:
      url: https://somewhere
      sha256: 123
    build:
      noarch: True
      number: 0
      run_exports:
        # add a run export
        # (note that jinja templating as in reality is not
        # supported by our test system to the same
        # extend as in conda build)
        - "{{ pin_subpackage('one') }}"
    test:
      commands:
        - do nothing
    about:
      license: GPLv3 # offending entry
      home: https://elsewhere
      summary: the_summary