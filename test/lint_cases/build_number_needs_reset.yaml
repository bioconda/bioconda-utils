expected_failures:
  - build_number_needs_reset
repodata:
  bioconda:
    one:
      - version: 0.0.1
recipes:
  one:
    package:
      name: one
      version: 0.1.0
    source:
      url: https://somewhere
      sha256: 123
    build:
      noarch: True
      number: 1 # offending entry
      run_exports:
        # add a run export
        # (note that jinja templating as in reality is not
        # supported by our test system to the same
        # extend as in conda build)
        - "{{ pin_subpackage('one') }}"
    test:
      commands:
        - do nothing
    about:
      license: BSD
      home: https://elsewhere
      summary: the_summary