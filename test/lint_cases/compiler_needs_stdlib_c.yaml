expected_failures:
  - compiler_needs_stdlib_c
recipes:
  one:
    package:
      name: one
      version: 0.1
    source:
      url: https://somewhere
      sha256: 123
    build:
      number: 0
      run_exports:
        - "{{ pin_subpackage('one') }}"
    requirements:
      build:
        - '{{compiler("c")}}'
    test:
      commands:
        - do nothing
    about:
      license: BSD
      home: https://elsewhere
      summary: the_summary
